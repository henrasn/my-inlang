# TODO

[X] export to mobile
[X] export each platform separately
[X] sync downstream to existing project
[X] sync upstream from mobile
[] sync from inlang repo when build app
[] sync to inlang repo from mobile
